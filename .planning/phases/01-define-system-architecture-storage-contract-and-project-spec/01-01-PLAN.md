---
phase: 01-define-system-architecture-storage-contract-and-project-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/insta-creator-system-spec.md
  - docs/insta-creator-project-spec-contract.md
  - docs/insta-creator-storage-contract.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-06
must_haves:
  truths:
    - The shared skill suite boundary is clearly separated from project-specific branding.
    - The per-post folder contract is documented with required artifacts.
    - The bootstrap scope is explicitly limited to the shared suite.
  artifacts:
    - docs/insta-creator-system-spec.md
    - docs/insta-creator-project-spec-contract.md
    - docs/insta-creator-storage-contract.md
  key_links:
    - The project-spec contract must be consumable by later workflow skills without modifying the bootstrap.
    - The storage contract must define a deterministic per-post folder name and required file set.
---

<objective>
Define the architecture contract for the reusable Instagram content system.

Purpose: Establish the shared workflow boundaries, the project-spec contract, and the deterministic storage layout before implementation begins.
Output: Three durable design docs that later phases can implement against.
</objective>

<execution_context>
@$HOME/.hermes/get-shit-done/workflows/execute-plan.md
@$HOME/.hermes/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-define-system-architecture-storage-contract-and-project-spec/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the system architecture spec</name>
  <files>docs/insta-creator-system-spec.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-define-system-architecture-storage-contract-and-project-spec/01-CONTEXT.md</read_first>
  <action>Document the shared workflow architecture in a project-agnostic way. Include: shared vs project-specific boundary, cron-fed idea intake, per-post storage model, and the role of the bootstrap. Keep the wording generic enough to reuse across future projects.</action>
  <verify>grep for the sections "Shared workflow boundary", "Project-specific spec", "Per-post artifact storage", and "Bootstrap scope".</verify>
  <acceptance_criteria>
    - docs/insta-creator-system-spec.md exists
    - It clearly states that branding belongs outside the shared bootstrap
    - It documents the per-post folder contract
  </acceptance_criteria>
  <done>The architecture spec is readable as the canonical shared-system description.</done>
</task>

<task type="auto">
  <name>Task 2: Define the project-spec contract</name>
  <files>docs/insta-creator-project-spec-contract.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-define-system-architecture-storage-contract-and-project-spec/01-CONTEXT.md</read_first>
  <action>Define the contract that each project spec must satisfy. Include required sections for voice, visual rules, CTA conventions, prohibited angles, and approval behavior. Make it project-agnostic but explicit enough for later skills to parse and apply.</action>
  <verify>grep for the sections "Required fields", "Voice", "Visual rules", "CTA conventions", and "Approval behavior".</verify>
  <acceptance_criteria>
    - docs/insta-creator-project-spec-contract.md exists
    - The contract can be reused by GranaFlow and future brands
    - The bootstrap is explicitly not responsible for storing brand-specific rules
  </acceptance_criteria>
  <done>The project-spec contract is explicit and reusable across brands.</done>
</task>

<task type="auto">
  <name>Task 3: Define the storage contract</name>
  <files>docs/insta-creator-storage-contract.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-define-system-architecture-storage-contract-and-project-spec/01-CONTEXT.md</read_first>
  <action>Document the deterministic per-post folder layout and file inventory. Include the naming convention, required files, asset subfolder rules, and traceability fields in the manifest. Keep it implementation-ready for later workflow skills.</action>
  <verify>grep for the sections "Folder naming", "Required files", "assets/", and "manifest.json".</verify>
  <acceptance_criteria>
    - docs/insta-creator-storage-contract.md exists
    - The per-post folder name is deterministic
    - The required artifact set is unambiguous
  </acceptance_criteria>
  <done>The storage contract can be used as a build target by later phases.</done>
</task>

</tasks>

<verification>
- [ ] The three docs exist in the repository
- [ ] Each doc contains the exact contract sections described above
- [ ] `.planning/STATE.md` records the phase-1 design decisions
</verification>

<success_criteria>
- All three architecture-contract docs are created
- The shared-vs-project boundary is explicit
- The storage layout is deterministic and reusable
- The phase is ready for later skill implementation
</success_criteria>
